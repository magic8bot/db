{
  "name": "@magic8bot/db",
  "version": "2.0.6",
  "description": "Cryptocurrency trading bot",
  "bugs": "https://github.com/magic8bot/db/issues",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/magic8bot/db.git"
  },
  "main": "./dist/index.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "tsc",
    "dist": "npm run build",
    "rebuild": "npm run clean && npm run dist",
    "prepub": "npm run rebuild",
    "pub": "npm publish --access=public"
  },
  "dependencies": {
    "cross-env": "7.0.2",
    "dotenv": "^8.2.0",
    "mongodb": "^3.6.3"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/mongodb": "^3.6.3",
    "@types/node": "^14.14.14",
    "prettier": "^2.2.0",
    "rimraf": "^3.0.2",
    "tslint": "^6.1.3",
    "typescript": "^4.1.2"
  }
}